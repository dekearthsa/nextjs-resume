export interface CardData {
  title: string;
  imagePath: string;
  jobs: string[];
  protocal: string[];
  types: string;
}

export interface StructProject {
  name: string;
  img: string;
  desc: string;
}
